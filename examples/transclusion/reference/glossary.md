# Glossary

## Atomic note

One idea, one file, named for the idea rather than for the
document it might end up in.

## Transclusion

Including one document's contents inside another by reference,
so the text has exactly one home and any number of appearances.

## Composition file

A short, rearrangeable table of contents whose body is mostly
links. Moving a section is `dd` then `p`.
